---
import CircleText from "../components/CircleText";
export interface Props {
	title: string;
}

const { title } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>CodeF 2023 - {title}</title>
	</head>
	<body>
		<header
			class="bg-gradient-to-r from-purple-400 via-pink-500 to-red-500 mb-3 p-3"
		>
			<div class="flex flex-col justify-center items-center">
				<nav class="block text-white font-mono font-extralight text-sm">
					<a href="/about">./About</a>
				</nav>
			</div>
		</header>

		<slot />

		<footer class="bg-gray-800 text-gray-500 body-font">
			<div
				class="container px-5 py-2 mx-auto flex items-center sm:flex-row flex-col"
			>
				<p class="text-sm text-gray-500 sm:ml-6 sm:mt-0">
					© 2023 Codef
				</p>
			</div>
		</footer>
	</body>
</html>
<style>
	:root {
	}

	html {
		font-family: system-ui, sans-serif;
		font-size: var(--font-size-base);
		color: var(--color-text);
		background-color: var(--color-bg);
	}
</style>
